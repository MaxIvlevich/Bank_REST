# ===============================================================
# Example Environment Variables for the Banking Card Application
# ===============================================================
#
# Copy this file to .env and fill in your actual values.
# The .env file is ignored by Git and should NOT be committed.
#

# --- Database Configuration ---
# These variables are used by the PostgreSQL container and the Spring Boot application.
POSTGRES_DB=banking_db
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_strong_db_password

# --- Application Configuration ---
# These variables are used by the Spring Boot application inside the Docker container.
# The datasource URL is pre-configured for Docker Compose networking.
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-db:5432/${POSTGRES_DB}
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_LIQUIBASE_ENABLED=true

# --- Security & JWT Configuration ---
# Generate your own secure keys for production environments.
# ENCRYPTION_KEY must be exactly 16, 24, or 32 characters long.
ENCRYPTION_KEY=change_this_to_16_char_secret_key
# APP_JWT_SECRET should be a long, random, Base64-encoded string.
APP_JWT_SECRET=change_this_to_your_base64_jwt_secret_key_very_long

# Token validity durations in milliseconds
# 900000ms = 15 minutes
ACCESS_TOKEN_TIME=900000
# 604800000ms = 7 days
REFRESH_TOKEN_TIME=604800000

# --- Initial Admin User ---
# Credentials for the default admin user created on first startup.
LOGIN_INIT_USER=admin
PASS_INIT_USER=admin
EMAIL_INIT_USER=admin@admin
LASTNAME_INIT_USER=admin
FIRSTNAME_INIT_USER=admin

